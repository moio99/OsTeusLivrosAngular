import { FiltroListDragPipe } from './filtro-list-drag.pipe';

describe('FiltroListDragPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroListDragPipe();
    expect(pipe).toBeTruthy();
  });
});
